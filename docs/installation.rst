============
Installation
============

At the command line either via easy_install or pip::

    $ easy_install evolving_threat_detector
    $ pip install evolving_threat_detector

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv evolving_threat_detector
    $ pip install evolving_threat_detector
